# 🌀 PROJECT JANUS: GENESIS

> *"Это не игра. Это зеркало твоего подсознания."*

**Janus Genesis (v7.0)** — это бесконечная, саморазвивающаяся текстовая RPG-песочница (Interactive Fiction), работающая на базе нейросетей (Gemini 1.5/2.0). Здесь нет заранее прописанного сценария. Мир генерируется в реальном времени, реагируя на ваши действия, страхи и психотип.

![Python](https://img.shields.io/badge/Python-3.x-blue) ![AI](https://img.shields.io/badge/Backend-Gemini_API-orange) ![Entropy](https://img.shields.io/badge/System-Dynamic-red)

---

## 🧠 Что делает Януса особенным?

В отличие от обычных чат-ботов, Janus обладает **памятью, состоянием и эмпатией**. Он не просто отвечает на вопросы, он ведет вас через "Глубину" (Depth), выступая в роли безмолвного Данжен-Мастера.

### 🔥 Ключевые механики (Core Mechanics):

**1. Adaptive Psyche Engine (Эмпатический движок)**
Система анализирует ваш ввод с помощью нечеткой логики (Fuzzy Logic).
* Если вы агрессивны — мир становится жестче и враждебнее.
* Если напуганы — мир давит на психику (хоррор).
* Если любопытны — раскрывает лор и тайны.
* *Код:* `state.psych_profile` и `action_history` адаптируются под ваш стиль игры.

**2. Dynamic Entropy Flux (Динамическая Энтропия)**
Уровень хаоса больше не растет линейно. Мир реагирует на контекст:
* **Стресс/Битва/Глитчи:** Энтропия резко растет (`↑0.15`). Реальность рушится.
* **Покой/Артефакты/Решения:** Энтропия снижается (`↓0.10`). Мир стабилизируется.
* *UI:* Вы видите дельту изменений в реальном времени.

**3. Persistent World State (Вечная Память)**
Весь прогресс (Инвентарь, Лор, Психопрофиль, Глубина) сохраняется в локальный JSON (`janus_world_state.json`). Вы можете найти "Ключ" сегодня, а дверь для него — через неделю.

**4. Bulletproof Stability**
Используется гибридный парсер (JSON + Regex Fallback). Если Нейросеть "галлюцинирует" или ломает формат, игра не вылетает, а сюжетно обыгрывает сбой связи.

---

## 🛠 Технический стек

* **Язык:** Python 3.x (Zero-dependency architecture).
* **AI Backend:** Google Gemini (1.5 Flash / 1.5 Pro) с ротацией ключей и экспоненциальной задержкой (Backoff) при сбоях.
* **Security:** Поддержка `.env` файлов "из коробки" (без внешних библиотек).

---

## 🚀 Установка и Запуск

### 1. Клонирование
```bash
git clone [https://github.com/your-username/janus-genesis.git](https://github.com/your-username/janus-genesis.git)
cd janus-genesis

2. Зависимости
Вам понадобится только библиотека requests:
pip install requests

3. Запуск
python janus_genesis.py

(Или просто запустите файл в Termux / a-Shell на iOS)


4. Конфигурация (Security)
При первом запуске система сама предложит ввести Google Gemini API Keys. Они будут сохранены в файл .env (скрытый файл конфигурации).

Важно: Файл .env автоматически добавлен в .gitignore, ваши ключи останутся только на вашем устройстве.


🎮 Как выжить в Системе?
Свобода воли: Вы не ограничены предложенными вариантами. Пишите что угодно: "Съесть синюю таблетку" или "Попытаться перепрограммировать матрицу". Янус подстроится.

Следите за параметрами:

DEPTH: Как далеко вы ушли от реальности (прокачивается через Lore).

ENTROPY: Уровень искажения реальности (влияет на сложность и абсурдность).

Ищите Истину: Иногда система выдает Lore (куски сюжета) или Artifacts (предметы с активными способностями).
> Твои действия: Осмотреть странный обелиск
Твой текущий README хорош по духу, но технически устарел относительно версий v6.0/v7.0, которые мы сделали.

Что устарело:

Безопасность ключей: Мы больше не используем janus_keys.json (это небезопасно). Теперь мы используем .env (стандарт индустрии).

Энтропия: Она больше не просто "растет" (Escalation). Теперь она Динамическая (Dynamic Flux) — может падать и скакать в зависимости от контекста.

Стабильность: Стоит упомянуть новый движок парсинга (Regex), который не дает игре вылетать.

Вот обновленная версия README, актуальная для твоего кода v7.0. Я сохранил твой кибер-мистический стиль, но поправил технические факты.

Markdown

# 🌀 PROJECT JANUS: GENESIS

> *"Это не игра. Это зеркало твоего подсознания."*

**Janus Genesis (v7.0)** — это бесконечная, саморазвивающаяся текстовая RPG-песочница (Interactive Fiction), работающая на базе нейросетей (Gemini 1.5/2.0). Здесь нет заранее прописанного сценария. Мир генерируется в реальном времени, реагируя на ваши действия, страхи и психотип.

![Python](https://img.shields.io/badge/Python-3.x-blue) ![AI](https://img.shields.io/badge/Backend-Gemini_API-orange) ![Entropy](https://img.shields.io/badge/System-Dynamic-red)

---

## 🧠 Что делает Януса особенным?

В отличие от обычных чат-ботов, Janus обладает **памятью, состоянием и эмпатией**. Он не просто отвечает на вопросы, он ведет вас через "Глубину" (Depth), выступая в роли безмолвного Данжен-Мастера.

### 🔥 Ключевые механики (Core Mechanics):

**1. Adaptive Psyche Engine (Эмпатический движок)**
Система анализирует ваш ввод с помощью нечеткой логики (Fuzzy Logic).
* Если вы агрессивны — мир становится жестче и враждебнее.
* Если напуганы — мир давит на психику (хоррор).
* Если любопытны — раскрывает лор и тайны.
* *Код:* `state.psych_profile` и `action_history` адаптируются под ваш стиль игры.

**2. Dynamic Entropy Flux (Динамическая Энтропия)**
Уровень хаоса больше не растет линейно. Мир реагирует на контекст:
* **Стресс/Битва/Глитчи:** Энтропия резко растет (`↑0.15`). Реальность рушится.
* **Покой/Артефакты/Решения:** Энтропия снижается (`↓0.10`). Мир стабилизируется.
* *UI:* Вы видите дельту изменений в реальном времени.

**3. Persistent World State (Вечная Память)**
Весь прогресс (Инвентарь, Лор, Психопрофиль, Глубина) сохраняется в локальный JSON (`janus_world_state.json`). Вы можете найти "Ключ" сегодня, а дверь для него — через неделю.

**4. Bulletproof Stability**
Используется гибридный парсер (JSON + Regex Fallback). Если Нейросеть "галлюцинирует" или ломает формат, игра не вылетает, а сюжетно обыгрывает сбой связи.

---

## 🛠 Технический стек

* **Язык:** Python 3.x (Zero-dependency architecture).
* **AI Backend:** Google Gemini (1.5 Flash / 1.5 Pro) с ротацией ключей и экспоненциальной задержкой (Backoff) при сбоях.
* **Security:** Поддержка `.env` файлов "из коробки" (без внешних библиотек).

---

## 🚀 Установка и Запуск

### 1. Клонирование
```bash
git clone [https://github.com/your-username/janus-genesis.git](https://github.com/your-username/janus-genesis.git)
cd janus-genesis
2. Зависимости
Вам понадобится только стандартная библиотека requests:

Bash

pip install requests
3. Запуск
Bash

python janus_genesis.py
(Работает на PC, Mac, Linux и мобильных терминалах Termux / a-Shell)

4. Конфигурация (Security)
При первом запуске система сама предложит ввести Google Gemini API Keys. Они будут сохранены в файл .env (скрытый файл конфигурации).

Важно: Файл .env автоматически добавлен в .gitignore, ваши ключи останутся только на вашем устройстве.

🎮 Как выжить в Системе?
Свобода воли: Вы не ограничены предложенными вариантами. Пишите что угодно: "Съесть синюю таблетку" или "Попытаться перепрограммировать матрицу". Янус подстроится.

Следите за параметрами:

DEPTH: Как далеко вы ушли от реальности (прокачивается через Lore).

ENTROPY: Уровень искажения реальности (влияет на сложность и абсурдность).

Ищите Истину: Иногда система выдает Lore (куски сюжета) или Artifacts (предметы с активными способностями).

📜 Пример сессии (v7.0)
Plaintext

[DEPTH: 5 | ENTROPY: 0.45 (↑0.12) | PSYCH: Analytical]

> Твои действия: Ударить по зеркалу кулаком

🌀 Стекло не разбивается, а становится вязким, поглощая твою руку. Ты чувствуешь холодное рукопожатие с той стороны.
[!] ARTIFACT: Жидкое Серебро (Позволяет проходить сквозь стены)

Варианты путей:
1. Выдернуть руку силой.
2. Позволить зеркалу затянуть тебя целиком.
3. [Уникально] Использовать "Жидкое Серебро" на себе.

⚠️ Disclaimer
Проект является экспериментальной песочницей. ИИ может генерировать неожиданный, пугающий или абсурдный контент. Вы — единственный автор своего сна.

Created by Hawkar // Project Janus

